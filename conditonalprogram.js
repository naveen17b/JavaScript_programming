
// let theNumber = Number(prompt("Pick a number"));

// if (!Number.isNaN(theNumber)) {
// console.log("Your number is the square root of " +
// theNumber * theNumber);
// }

let theNumber = Number(prompt("Pick a number"));
if (!Number.isNaN(theNumber)) {
  console.log("The square root of your number is: " + Math.sqrt(theNumber));
}

/**
 * The prompt function is a method in web browsers that is used to interact with users by displaying a dialog box 
 * that prompts them to input text. It's commonly used in JavaScript to gather input from users in a simple and straightforward way.
 */


/**
 * Output of the program: we can't run this program as it is used ouside the browser
 */